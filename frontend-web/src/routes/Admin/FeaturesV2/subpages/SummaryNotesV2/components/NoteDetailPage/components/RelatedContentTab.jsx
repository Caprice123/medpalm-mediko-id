import AnatomyQuizLinkSection from './AnatomyQuizLinkSection'

function RelatedContentTab({ noteUniqueId }) {
  return <AnatomyQuizLinkSection noteUniqueId={noteUniqueId} />
}

export default RelatedContentTab
